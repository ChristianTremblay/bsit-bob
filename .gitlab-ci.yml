# SERVISYS inc. Gitlab-CI Configuration file
# Official language image. Look for the different tagged releases at:
# https://hub.docker.com/r/library/python/tags/
image: python:latest

# Change pip's cache directory to be inside the project directory since we can
# only cache local items.
variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

# Pip's cache doesn't store the python packages
# https://pip.pypa.io/en/stable/reference/pip_install/#caching
#
# If you want to also cache the installed packages, you have to install
# them in a virtualenv and cache it as well.
cache:
  paths:
    - .cache/pip
    - venv/

before_script:
  - python -V               # Print out python version for debugging
  - pip install virtualenv
  - virtualenv venv
  - source venv/bin/activate

test_python:
  script:
    - pip install .
    - pip install pytest
    - pip install pyshacl
    - pip install ontoenv
    - pip install rdflib
    - pip install pyvis
    - pytest
#    - cd tests
#    - bash no-barf.sh

#validate_pritoni:
#  script: 
#    - pip install .
#    - pip install pytest
#    - pip install pyshacl
#    - pip install ontoenv
#    - pip install rdflib
#    - ontoenv refresh
#    - ["bash ./validate-pritoni.sh"]
#  artifacts:
#    expose_as: 'Report'
#    paths: ['samples/ttl/sample_pritoni_model.report.ttl']

#test:
#  stage: test
#  script:
#    - apt-get update -qy
#    - apt-get install -y python3-dev python3-pip zip unzip git
#    - pip3 install -r tests/requirements.txt#
#    - ontoenv ini#t
#    - cd tests
#    - pytest -s -vvvv --durations=0 -n auto
